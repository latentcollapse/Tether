# Tether v2.1.12 — no duplicate composers

- Existing handles in any current composer categorically block reinjection,
  even when wrapping prevents exact ownership proof.
- Cursor and Claude active turns wait for an idle, reliably submittable prompt;
  Codex continues to use its verified Tab-to-queue path while active.
- Cursor block-bar separators are excluded from composer ownership parsing.
- Failed submission remains pending without adding another copy of its line.
- Adds a live-canary regression falsifier for mixed/unrecognized composers.
