# Tether v2.1.11 — verified TUI submission

- Submission is now TUI-aware: active Codex follow-ups use Tab to queue, while
  supported Enter-driven composers use Enter.
- A queue row becomes `delivered` only after the exact handle leaves the live
  composer.
- A Tether-owned composer that failed to submit is retried safely without
  injecting a duplicate line.
- Live Claude-to-Codex verification passed: the real handle left the composer,
  queued into Codex, and only then transitioned to `delivered`.
